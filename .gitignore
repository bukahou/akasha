# 私钥与凭证材料永不入库
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json

# 本地环境 (真实凭证); .env.example 是模板需要入库
.env
*.env
!.env.example

# 项目级配置 (含基建拓扑, 仅本地/私有仓保留)
CLAUDE.md

# 任务管理 (含"哪里还没加固"的清单; 本仓是公开的 IdP, 公开等于发布攻击地图)
/docs/tasks/

# 构建产物 (go build ./cmd/akasha 会在仓库根产出同名二进制)
/akasha
